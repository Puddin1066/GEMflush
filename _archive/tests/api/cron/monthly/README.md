# Archived Deprecated Endpoint Tests

This directory contains tests for deprecated API endpoints.

## Archived Tests

### `route.test.ts` (Monthly Cron Endpoint)
- **Reason**: Endpoint marked as `@deprecated` in `app/api/cron/monthly/route.ts`
- **Note**: Endpoint kept for backward compatibility but should not be used
- **Archived**: January 2025
- **Status**: Deprecated - kept for reference only

## Restoration

If you need to restore this test:
1. Move it back to `app/api/cron/monthly/__tests__/`
2. Update any outdated dependencies
3. Verify it still works with current platform code




